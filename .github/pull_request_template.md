## What this PR does
<!-- One paragraph. What exists now that didn't before, or what is fixed. -->

## Outcome and execution links
- GitHub outcome issue: #
- Root HQ Kanban task ID:
- Kanban tenant:
- Root graph URL:

## Issue relation
<!-- Use exactly one relation below. Partial work advances an issue; only a complete outcome closes it. -->
Refs/Advances #
<!-- Replace the line above with `Closes #N` only when this merge completes the whole issue. -->

## Layer(s) affected
- [ ] Layer 0 — Data & universe
- [ ] Layer 1 — Features
- [ ] Layer 2 — Model
- [ ] Layer 3 — Portfolio
- [ ] Layer 4 — Risk
- [ ] Layer 5 — Execution
- [ ] Infrastructure / services
- [ ] Tests only
- [ ] Docs only

## Author
- [ ] Written by me
- [ ] Generated by code-monkey — reviewed and approved by me

## Governance evidence
- Independent review task/profile and verdict:
- Domain acceptance (semantic/backend scope):
- Exceptional human gate decision, if applicable (schema migration, Stock-tab semantic evidence, consequential broad backfill, or other outcome-specific gate):
- Current-head merge gates: required CI/checks; fresh independent PASS with zero Blocking findings; mergeable/CLEAN head; applicable repository-specific migration/release/deployment/security/rollback gates:

---

## code-monkey checklist
*code-monkey must verify every item before opening this PR*

### Correctness
- [ ] Output matches schema defined in `core/contracts/schemas.py`
- [ ] No logic added to forbidden files
  (`agent_execution.py`, `broker_adapter.py`, `order_builder.py`,
   `fills.py`, `risk_policy.json`, `portfolio_policy.json`)
- [ ] No hardcoded credentials, API keys, or absolute file paths
- [ ] No `print()` statements — logger used throughout
- [ ] No bare `except:` or silent exception swallowing

### Tests
- [ ] `./.venv/bin/pytest tests/unit/ -v --tb=short` passes with zero failures
- [ ] New public functions have at least one unit test each
- [ ] Tests cover: happy path, empty input, missing columns, NaN input
- [ ] No live API calls in unit tests — fixtures used from `data/sample/`

### Code quality
- [ ] All new public functions have type hints
- [ ] All new public functions have a docstring
- [ ] Imports ordered: stdlib → third-party → internal
- [ ] No unused imports

### Project hygiene
*The implementation worker verifies scope; the responsible domain owner owns GitHub lifecycle state.*
- [ ] Branch named `code-monkey/<issue-number>-<slug>` or `feature/<slug>`
- [ ] Domain owner has recorded the issue's current GitHub milestone after this PR opened
- [ ] PR references both the GitHub issue and root HQ Kanban task ID
- [ ] No unrelated files modified
- [ ] `requirements.txt` updated if new packages added (noted below)

---

## New dependencies
<!-- List any packages added to requirements.txt and why -->
None

## Screenshots or sample output
<!-- Optional but helpful for portfolio/risk/feature changes -->

## Notes for reviewer
<!-- Anything the human reviewer should pay particular attention to,
     or tradeoffs made during implementation -->
