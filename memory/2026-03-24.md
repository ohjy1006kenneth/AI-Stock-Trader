# 2026-03-24

- Built a deterministic US-stock mock trading system in the `trading` workspace with strict boundaries: `trading` owns scripts/outputs/ledger/reports/cron runtime; the project later simplified into a smaller specialist set under `trading`.
- Created persistent OpenClaw agents for the trading system and later renamed the specialist agents to `trading-*` prefixes to avoid future conflicts with non-trading coding agents.
- Added canonical routing config at `config/delegation_map.json` and documented that specialist agents are role workspaces only, not a second source of truth.
- Implemented deterministic trading pipeline scripts, including `portfolio_strategist.py` (decision-only), `mock_portfolio_executor.py` (sole ledger mutator), `backtest_engine.py` (real deterministic simulation), `portfolio_status.py` (read-only inspection), and automation helpers like `trade_alerts.py` and `preflight_check.py`.
- User preference: when asked for "portfolio status", run `python3 scripts/portfolio_status.py` and return the full detailed output, not a summary.
- Added cron automation for mock-only trading with America/Chicago timing: preflight alert at 6:09 PM weekdays, main after-close pipeline at 6:10 PM, mock trade alerts at 6:11 PM, and daily summary at 7:00 AM.
- Added production-safe runtime flow using `.venv/bin/python`, wrapper scripts, and preflight gating; latest recorded preflight status passed (`outputs/preflight_status.json` showed ok=true).
- User had already run the Python venv setup/install commands and asked for explicit next-step commands after setup: run `./scripts/run_pipeline.sh`, inspect `python3 scripts/portfolio_status.py`, inspect `outputs/execution_log.json`, inspect the newest `reports/daily_summary_*.md`, and verify cron via `openclaw cron list`.
