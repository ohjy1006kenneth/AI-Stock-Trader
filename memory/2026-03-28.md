# 2026-03-28

- Restructured the trading workspace into a GitHub-ready repo layout and pushed the changes. Key stable layout now separates runtime (`runtime/pi`), shared helpers (`runtime/common`), strategy logic (`strategy/`), backtests (`backtests/engine`), generated runtime state (`data/runtime/`), ledger, and reports. Important follow-up fixes were also pushed for refactor regressions and cron entrypoint updates.
- Updated active agent behavior/ownership to match the cloud-training + Pi-runtime plan. Current active agents: `trading` (orchestrator), `trading-quant-researcher` (research/features/model ideas), `trading-backtest-validator` (validation/promotion gate), `trading-portfolio-strategist` (decision policy), `trading-executor-reporter` (paper execution/reporting).
- Removed the old 7:00 AM `trading-daily-summary` cron. Kept the after-close trading pipeline cron and later fixed its message payload so it points to the refactored runtime wrapper instead of the dead old `./scripts/run_pipeline.sh` path.
- Migrated execution from mock-oriented flow to Alpaca paper-trading integration. Runtime is now paper-only: active executor path is `runtime/pi/execution/paper_portfolio_executor.py`, local credentials live in gitignored `config/alpaca.env`, and pipeline wrapper loads that env file automatically.
- Verified Alpaca paper connectivity and full paper runtime: preflight passes, broker connectivity test works, and the pipeline successfully submitted a paper BUY order for GOOGL. Observed behavior: order was accepted but not yet filled outside market hours, so portfolio status remained flat until fill.
- Expanded the active universe from a hardcoded ~20-ticker seed list to S&P 500 scale. First implementation used live Wikipedia with fallback; then upgraded to a proper refresh-and-snapshot design.
- Current S&P 500 universe workflow: explicit refresh script `runtime/pi/data/refresh_sp500_constituents.py` fetches Wikipedia S&P 500 membership, intersects it with Alpaca active tradable U.S. equities, and writes deterministic runtime snapshot `config/sp500_constituents.json`. Normal runtime universe build now reads only that local snapshot and fails clearly if it is missing/invalid.
- Latest measured universe stats after S&P 500 expansion: old universe size 20; new snapshot-driven universe size 503; usable price data 503; usable fundamental data 503; quality-pass count 40; strong alpha count 9.
- GitHub repo linked and regularly pushed during significant changes. Recent notable pushed commits included repo restructure, agent-behavior update, paper-only execution cutover, S&P 500 expansion, and snapshot refresh workflow.
