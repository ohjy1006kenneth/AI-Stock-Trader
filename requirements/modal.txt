# Modal / cloud dependencies — heavy ML stack
# Install inside Modal container images (app/lab/, app/cloud/).
# Do NOT install on Pi — too large and unnecessary there.

-r base.txt

# Modal SDK — for defining and running cloud jobs
modal>=0.64,<1

# Data handling
pandas>=2.2,<3
numpy>=1.26,<3
pyarrow>=17,<18         # Parquet read/write

# Market data — Polygon (for cloud-side backtest data pulls)
polygon-api-client>=1.14,<2

# Object storage — Cloudflare R2
boto3>=1.35,<2

# --- NLP / Text branch ---
# FinBERT sentiment model
torch>=2.4,<3
transformers>=4.44,<5

# Sentence Transformers (all-mpnet-base-v2 for relevance filtering)
sentence-transformers>=3.1,<4

# Topic modeling
bertopic>=0.16,<1
umap-learn>=0.5,<1      # dimensionality reduction for embeddings

# --- Predictive model ---
xgboost>=2.1,<3
scikit-learn>=1.5,<2    # calibration (Platt scaling), preprocessing
shap>=0.46,<1           # feature importance monitoring

# --- Regime detection ---
hmmlearn>=0.3,<1        # Hidden Markov Model

# --- Portfolio construction ---
scipy>=1.14,<2          # mean-variance optimizer
