# cloud_inference

Cloud Oracle inference package.

This folder contains the inference-side code that loads trained artifacts, adapts runtime inputs into model-ready features, and returns structured outputs to the edge runtime.

This is for serving / scoring logic, not heavy training.
