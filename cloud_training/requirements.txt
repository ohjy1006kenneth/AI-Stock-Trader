# Cloud-side training / feature-generation dependencies only.
# Keep heavyweight NLP/ML dependencies out of the Pi runtime.

--extra-index-url https://download.pytorch.org/whl/cpu
# Newer transformers releases have started requiring newer torch behavior than our
# approved CPU-only 2.3.1 build. Pin to a known-working 4.x line for FinBERT.
transformers==4.41.2
# Prefer CPU-only torch for FinBERT feature generation in cloud/dev environments.
torch==2.3.1
numpy==2.4.4
pandas==2.2.3
scikit-learn==1.8.0
xgboost==3.2.0
